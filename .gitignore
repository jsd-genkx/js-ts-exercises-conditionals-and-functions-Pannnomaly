# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies node_modules จะไม่เอาขึ้นเพราะใหญ่
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc Ds_Store จะไม่เอาขึ้นเพราะใหญ่
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
# .env เราจะไม่เอาขึ้น เพราะมันเอาไว้เก็บ password, api key, database คือของสำคัญนั่นแหละ ห้ามหลุด
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# OS generated files
Thumbs.db
ehthumbs.db

# IDE/editor settings
# .vscode เราจะไม่เอาขึ้นด้วย เพราะมันเป็น setting ของ vscode
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Jest test results
jest-test-results.json

# Docker files
**/Dockerfile
**/docker-compose.yml

# Swagger generated files
swagger-ui/